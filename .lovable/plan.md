本次只调整气象看板相关文件，不重构其他模块、不改其他页面入口。

1. 数据源与顶部筛选
- 气象数据源只保留：
  - EC：公开API
  - 中科天机：页面抓取
- 在气象看板顶部增加一行筛选器：
  - 数据源切换：EC / 中科天机
  - 字段分组切换：光伏相关 / 负荷相关 / 降水相关 / 风电相关
  - 点位口径切换：全省代表值 / 新能源场站或区域点位 / 负荷中心点位
  - 时间粒度切换：小时 / 15分钟 / 日内
- 这些筛选先作为首版看板口径表达，不接入真实后端数据。

2. 气象字段分组收敛
- 不展示中科天机全部字段，只按交易用途展示字段组。
- 光伏相关：直接辐射、短波辐射、低云量、中云量、高云量、总云量。
- 负荷相关：2米气温、2米相对湿度、2米露点温度。
- 降水相关：降雨量、地表总降水率。
- 风电相关：10米风、100米风，并标注“风速由 U/V 分量计算”。
- 截图字段会明确保留：直接辐射、2米露点温度、2米气温、2米相对湿度、低云量、降雨量。

3. 核心气象卡片区
- 在顶部筛选下方新增“核心气象卡片区”。
- 展示 8-12 个交易相关字段，例如：
  - 直接辐射
  - 短波辐射
  - 低云量
  - 总云量
  - 2米气温
  - 2米相对湿度
  - 2米露点温度
  - 降雨量
  - 地表总降水率
  - 10米风速
  - 100米风速
  - 综合预警等级
- 卡片上保留轻量来源标识：公开API / 页面抓取 / 规则计算。

4. 预警区与规则说明
- 新增预警区，展示：
  - 当前预警等级
  - 触发原因
  - 影响区域
  - 影响对象：光伏 / 风电 / 负荷 / 市场波动
- 文案统一为“规则计算”，不写成 AI 极端天气预测。
- 阈值文案改为“阈值配置预留”，不把阈值写死成唯一规则。
- 默认逻辑按你给的规则展示：
  - 业务阈值优先
  - 无业务阈值时使用近30/90天同小时段 P90/P95 兜底
  - 数据源自带官方预警等级时优先展示官方等级

5. 点位口径与预警升级
- 在页面中明确区分三类点位口径：
  - 全省代表值
  - 新能源场站 / 区域点位
  - 负荷中心点位
- 预警升级逻辑在页面中以规则说明和示例结果表达：
  - 单个关键点位超过阈值：区域提示
  - 多个区域同时超过阈值：市场级提示
  - 气象异常与新能源预测偏差或负荷偏差同向叠加：提高提醒优先级

6. 影响说明与联动提示
- 新增“影响说明区”，把天气异常转成交易前判断语言：
  - 更可能影响光伏
  - 更可能影响风电
  - 更可能影响负荷
  - 更可能造成市场波动
- 保留并强化联动提示：
  - 气象 → 新能源预测 → 市场数据 → 竞价空间 → 电价关系 → 策略
- 当气象异常与新能源预测偏差或负荷偏差同向时，提示“提高提醒优先级”。

7. 24小时矩阵调整
- 保留 24 小时矩阵，但字段从纯天气字段改为交易字段口径。
- 表头会覆盖核心保留字段，并增加影响对象 / 预警等级 / 来源标识。
- 页面整体表达从“天气信息页”调整为“交易前判断入口”。

技术说明
- 主要修改：`src/pages/WeatherBoard.tsx`。
- 如需补充 mock 数据结构，会仅在 `src/lib/marketMocks.ts` 增加气象相关字段、分组、规则说明和示例数据。
- 不改市场看板、算法预测、交易日志、插件管理、侧栏结构等其他模块。